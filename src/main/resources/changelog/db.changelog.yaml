databaseChangeLog:
  - changeSet:
      id: 2
      author: TrucNguyen
      comment: "Create table product"
      changes:
        - sql:
            sql: CREATE TABLE IF NOT EXISTS ${schema}.product_task (id varchar(255) primary key, name varchar(255), price decimal)